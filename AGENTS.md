# Codex Notes

- Publish this project to its own GitHub repository, not to `ops-rush`.
- Deployment repository: `qq365098020/price-action-review-app`.
- After project changes, publish with `bash deploy.sh "<commit message>"`.
- Report the generated version number and GitHub Pages URL after publishing.
