# Codex Notes

- Publish this project to its own GitHub repository, not to `ops-rush`.
- Deployment repository: `qq365098020/bedside-cabinet-notes`.
- During private development, `bash deploy.sh "<commit message>"` only pushes to the private repository unless GitHub Pages is enabled separately.
- Report the generated version number. Report a GitHub Pages URL only when Pages is actually enabled.
